x-shared: &shared-node
  image: node:20-alpine
  working_dir: /usr/src/app
  environment:
    NODE_ENV: development
  volumes:
    - ./backend/src:/usr/src/app/src
    - ./backend/package.json:/usr/src/app/package.json
    - ./backend/package-lock.json:/usr/src/app/package-lock.json
    - ./backend/tsconfig.json:/usr/src/app/tsconfig.json
    - ./backend/vitest.config.ts:/usr/src/app/vitest.config.ts
    - melenotes_backend_node_modules:/usr/src/app/node_modules
  restart: unless-stopped

services:

  # ── Database ──────────────────────────────────
  postgres:
    image: postgres:16-alpine
    container_name: melenotes-postgres
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: taskflow
      POSTGRES_PASSWORD: taskflow
      POSTGRES_DB: taskflow
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U taskflow"]
      interval: 5s
      timeout: 5s
      retries: 5

  # ── Cache / Sessions ─────────────────────────
  redis:
    image: redis:7-alpine
    container_name: melenotes-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  # ── Backend API ──────────────────────────────
  backend:
    <<: *shared-node
    container_name: melenotes-backend
    ports:
      - "3000:3000"
    command: >
      sh -c "
        npm install &&
        npx prisma generate &&
        npx prisma migrate deploy &&
        npm run dev
      "
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    env_file:
      - .env

  # ── Worker (Reminders / Cron) ────────────────
  worker:
    <<: *shared-node
    container_name: melenotes-worker
    command: sh -c "npm install && npx prisma generate && npm run dev:worker"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    env_file:
      - .env

  # ── Frontend (Vite Dev Server) ───────────────
  frontend:
    image: node:20-alpine
    container_name: melenotes-frontend
    working_dir: /usr/src/app
    restart: unless-stopped
    ports:
      - "5173:5173"
    command: sh -c "npm install && npm run dev"
    volumes:
      - ./frontend/src:/usr/src/app/src
      - ./frontend/public:/usr/src/app/public
      - ./frontend/package.json:/usr/src/app/package.json
      - ./frontend/package-lock.json:/usr/src/app/package-lock.json
      - ./frontend/tsconfig.json:/usr/src/app/tsconfig.json
      - ./frontend/vite.config.ts:/usr/src/app/vite.config.ts
      - ./frontend/index.html:/usr/src/app/index.html
      - melenotes_frontend_node_modules:/usr/src/app/node_modules
    depends_on:
      - backend
    env_file:
      - .env

  # ── Reverse Proxy ────────────────────────────
  nginx:
    image: nginx:alpine
    container_name: melenotes-nginx
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - backend
      - frontend

volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local
  melenotes_backend_node_modules:
    driver: local
  melenotes_frontend_node_modules:
    driver: local
